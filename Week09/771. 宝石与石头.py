# 771. 宝石与石头.py


class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        res = 0
        j = set(J)
        for ch in S:
            if ch in j:
                res += 1
        return res


class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        j = set(J)
        res = 0
        for c in S:
            if c in j:
                res += 1
        return res


class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        j = set(J)
        res = 0
        for c in S:
            if c in j:
                res += 1
        return res


class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        j = set(J)
        res = 0
        for c in S:
            if c in j:
                res += 1
        return res


def main():
    pass


if __name__ == '__main__':
    main()
